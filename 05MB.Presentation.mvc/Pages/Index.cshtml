@page
@model _05MB.Presentation.mvc.Pages.IndexModel
@{
    ViewData["Title"] = "Index";
    string Liked = "fa fa-heart fa-2x text-red";
    string NotLiked = "fa fa-heart-o fa-2x text-black-50";
}

<main class="posts-listing col-lg-12">
    <div class="container">
        <div class="row">

            @foreach (var article in Model.Articles)
            {
                <div class="post col-xl-6">
                    <div class="post-thumbnail">
                        <a asp-page="/ArticleDetails" asp-route-id="@article.Id">
                            <img src="@article.Img" alt="@article.Title" title="@article.Title" class="img-fluid">
                        </a>
                    </div>
                    <div class="post-details">
                        <div class="post-meta d-flex justify-content-between">
                            <div class="date meta-last">@article.CreationDate</div>
                            <div class="category">
                                <a asp-page="/ArticleDetails" asp-route-id="@article.Id">@article.ArticleCategory</a>
                            </div>
                            <a asp-page-handler="Like" asp-route-id="@article.Id" class="d-flex align-items-center">
                                <div class="meta-last">
                                    <i class="@if (article.HasLike)
                                                {
                                                    @Liked;
                                                }
                                                else
                                                {
                                                    @NotLiked;
                                                }">
                                    </i>@article.Like
                                </div>
                            </a>
                        </div>
                        <a asp-page="/ArticleDetails" asp-route-id="@article.Id">
                            <h3 class="h4">@article.Title</h3>
                        </a>
                        <p class="text-muted">
                            @article.ShortDescription
                        </p>
                        <footer class="post-footer d-flex align-items-center">
                            <div class="comments meta-last"><i class="icon-comment"></i>@article.CommentsCount</div>
                        </footer>
                    </div>
                </div>
            }

        </div>
    </div>
</main>